To compile this code do the following:

gcc -pthread --std=gnu99 -o line_processor main.c

text file input:

./line_processor < {filename]

manual input is done as follows ::
./line_processor

